## What does this PR do?

## Why?

## How was it tested?

- [ ] `pytest backend/tests` passes
- [ ] `ruff check backend` passes
- [ ] `npm run build` succeeds
- [ ] Manually verified on a running display (describe below / screenshot for UI changes)
